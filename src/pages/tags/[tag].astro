---
import { getCollection } from 'astro:content';
import PostList from '../../components/PostList.astro';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getTerms, slugify, sortPosts, type Post } from '../../lib/posts';

interface Props { term: string; posts: Post[]; }

export async function getStaticPaths() {
  const posts = await getCollection('posts');
  return getTerms(posts, 'tags').map(({ name, slug }) => ({
    params: { tag: slug },
    props: { term: name, posts: sortPosts(posts.filter((post) => post.data.tags.some((tag) => slugify(tag) === slug))) },
  }));
}

const { term, posts } = Astro.props;
---

<BaseLayout title={`Posts tagged ${term}`}>
  <section class="wrap archive">
    <p class="eyebrow">Tag</p>
    <h1>{term}</h1>
    <PostList posts={posts} />
  </section>
</BaseLayout>
