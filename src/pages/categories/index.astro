---
import { getCollection, getEntry } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getTerms } from '../../lib/posts';

const posts = await getCollection('posts');
const terms = getTerms(posts, 'categories');
const source = await getEntry('pages', 'category-archive');
---

<BaseLayout title={source?.data.title ?? 'Categories'}>
  <section class="wrap archive">
    <p class="eyebrow">Browse the journal</p>
    <h1>{source?.data.title ?? 'Categories'}</h1>
    <ul class="term-cloud" role="list">
      {terms.map((term) => (
        <li><a href={`/categories/${term.slug}/`}><span>{term.name}</span><b>{term.count}</b></a></li>
      ))}
    </ul>
  </section>
</BaseLayout>
